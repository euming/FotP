using UnityEngine;
using System.Collections;

//  all stuff that has to do with debugging the live game can live here. We can then easily remove this stuff for the finished product.
public class DebugReporter : MonoBehaviour {

    public bool inGameOnly = false;      //  use only tiles that are in game.

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
